/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  distDir: 'dist',
  assetPrefix: '/dpr-calc/',
  basePath: '/dpr-calc',
};

module.exports = nextConfig;
